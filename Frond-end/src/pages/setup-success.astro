---
import Layout from '../layouts/Layout.astro';

const title = "Tarjeta Agregada | CBTA 71";
---

<Layout title={title}>
    <div class="min-h-screen bg-gradient-to-br from-green-50 to-emerald-50 flex items-center justify-center p-4">
        <div class="bg-white rounded-3xl shadow-2xl p-8 md:p-12 max-w-md w-full text-center">
            <!-- Success Icon -->
            <div class="mb-6 flex justify-center">
                <div class="w-20 h-20 bg-green-100 rounded-full flex items-center justify-center animate-bounce">
                    <svg class="w-10 h-10 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"></path>
                    </svg>
                </div>
            </div>

            <!-- Title -->
            <h1 class="text-3xl md:text-4xl font-bold text-gray-900 mb-3">
                ¡Éxito!
            </h1>

            <!-- Message -->
            <p class="text-gray-600 mb-2 text-lg">
                Tu tarjeta ha sido agregada correctamente.
            </p>
            <p class="text-gray-500 text-sm mb-8">
                Serás redirigido automáticamente en <span id="countdown">5</span> segundos...
            </p>

            <!-- Details -->
            <div class="bg-green-50 border border-green-200 rounded-xl p-4 mb-8">
                <p class="text-green-800 font-semibold text-sm">
                    ✅ Transacción completada con Stripe
                </p>
                <p class="text-green-700 text-xs mt-2" id="sessionId">
                    <!-- Session ID will be added by JavaScript -->
                </p>
            </div>

            <!-- Button -->
            <a href="/Estudiante/Tarjetas" class="inline-block w-full bg-green-600 hover:bg-green-700 text-white font-bold py-3 px-6 rounded-xl transition-colors">
                Ir a Mis Tarjetas Ahora
            </a>

            <!-- Back Button -->
            <button onclick="window.history.back()" class="w-full mt-3 bg-gray-200 hover:bg-gray-300 text-gray-800 font-semibold py-2 px-4 rounded-lg transition-colors">
                Volver Atrás
            </button>
        </div>
    </div>

    <script is:inline>
        // Extract session_id from URL
        const params = new URLSearchParams(window.location.search);
        const sessionId = params.get('session_id');
        
        if (sessionId) {
            document.getElementById('sessionId').innerHTML = `
                <strong>ID de Sesión:</strong><br>
                <code style="font-size: 0.75rem; word-break: break-all;">${sessionId.substring(0, 30)}...</code>
            `;
            console.log('✅ Pago completado - Session ID:', sessionId);
        }

        // Auto-redirect countdown
        let seconds = 5;
        const countdownEl = document.getElementById('countdown');
        
        const interval = setInterval(() => {
            seconds--;
            countdownEl.textContent = seconds;
            
            if (seconds <= 0) {
                clearInterval(interval);
                window.location.href = '/Estudiante/Tarjetas';
            }
        }, 1000);

        // Log success
        console.log('✅ Página de éxito cargada');
        console.log('🔄 Redirigiendo a Tarjetas en 5 segundos...');
    </script>

    <style>
        @keyframes bounce {
            0%, 100% {
                transform: translateY(0);
            }
            50% {
                transform: translateY(-10px);
            }
        }

        .animate-bounce {
            animation: bounce 1s infinite;
        }

        code {
            font-family: 'Courier New', monospace;
            background-color: #f3f4f6;
            padding: 0.25rem 0.5rem;
            border-radius: 4px;
            display: inline-block;
            max-width: 100%;
            overflow-wrap: break-word;
        }
    </style>
</Layout>
