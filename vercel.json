{
  "buildCommand": "npm install && npm run build",
  "outputDirectory": ".vercel/output",
  "installCommand": "npm install",
  "functions": {
    "**/*.func/**": {
      "maxDuration": 30
    }
  }
}
